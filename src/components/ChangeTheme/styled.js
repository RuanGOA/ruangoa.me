import styled from 'styled-components';

export const ThemeButton = styled.button`
  cursor: pointer;

  background: transparent;

  border: 0;

  transition: all 0.5s;

  & > svg {
    width: 5em;
  }
`;
